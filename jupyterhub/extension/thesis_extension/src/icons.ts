import { LabIcon } from '@jupyterlab/ui-components';
import hyperledgerIcon from '../style/icons/hyperledger-icon.svg';

export const extensionIcon = new LabIcon({
  name: 'thesis-extension:hyperledger',
  svgstr: hyperledgerIcon
});